#!/usr/bin/env bash

load_config ./letsencrypt.conf

declare -A DOMAIN_FIELD=(
  [type]=url
  [name]=domain
  [value]=${CONFIG[domain]}
)
declare -A EMAIL_FIELD=(
  [type]=email
  [name]=email
  [value]=${CONFIG[email]}
)
declare -a FORM=(DOMAIN_FIELD EMAIL_FIELD)